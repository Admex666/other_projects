import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Core background colors
  static const Color background = Color(0xFF0F0F0F);
  static const Color surface = Color(0xFF1E1E1E);
  static const Color surfaceVariant = Color(0xFF2A2A2A);

  // Brand / Goal Types
  static const Color emerald = Color(0xFF10B981); // Financial
  static const Color indigo = Color(0xFF6366F1);  // Career/Work
  static const Color amber = Color(0xFFF59E0B);   // Mental/Spiritual
  static const Color crimson = Color(0xFFEF4444);  // Physical/Body
  static const Color rose = Color(0xFFF43F5E);     // Social/Relationship

  // Text
  static const Color textPrimary = Color(0xFFF9FAFB);
  static const Color textSecondary = Color(0xFF9CA3AF);
  static const Color textMuted = Color(0xFF6B7280);

  // Actions
  static const Color primary = emerald;
  static const Color accent = indigo;
}
